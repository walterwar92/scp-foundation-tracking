# Shell scripts must keep LF endings even on Windows checkouts
*.sh text eol=lf

# Windows batch and PowerShell scripts keep CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# SQL is portable text
*.sql text
